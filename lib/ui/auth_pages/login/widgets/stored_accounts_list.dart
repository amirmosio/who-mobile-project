import 'package:flutter/material.dart';

// TODO: Removed - Authentication widgets no longer needed
// Placeholder widget since authentication was removed
class StoredAccountsList extends StatelessWidget {
  const StoredAccountsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
