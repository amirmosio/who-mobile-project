import 'package:flutter/material.dart';

// TODO: Removed - Address form depends on userProfileProvider which was removed
// Placeholder widget
class AddressFormWidget extends StatelessWidget {
  const AddressFormWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Address form functionality has been removed'),
    );
  }
}
